# Installation

To install the stable release from PyPI:

```bash
pip install netbayesabm
```

For development (latest code from GitHub):

```bash
git clone https://github.com/galeanojav/NetBayesABM.git
cd NetBayesABM
pip install -e .
```
